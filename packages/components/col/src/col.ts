import { buildProps } from "@my-element-plus/utils"
export const colProps = buildProps({
    span: {
        type:  Number,
        default: 24
    },
    offset: {
        type:  Number,
        default: 0
    }
})
